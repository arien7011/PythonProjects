# What will be the type of s = {}
s = {}
print(type(s))

#so the type will be of dictionary because in python until you s is empty it will be of type dict so if you want to define an
# 'empty set' then you can define like that
# s = set() so now this is an empty set and it will of type of set